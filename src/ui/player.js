/**
 * player.js — Result playback and download
 *
 * Exports:
 * - showResult(originalFile: File, resultAudio: Float32Array, sampleRate: number, stats: object) → void
 * - hideResult() → void
 */

// TODO: Worker 2 (UI) will implement this
